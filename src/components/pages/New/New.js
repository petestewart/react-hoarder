import React from 'react';
// import PropTypes from 'prop-types';

import './New.scss';

const New = (props) => {
  return (
    <div>
      NEW
    </div>
  );
};

// New.propTypes = {}

export default New;
