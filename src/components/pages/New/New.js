import React from 'react';
// import PropTypes from 'prop-types';

import './New.scss';

const New = (props) => {
  return (
    <h1>
      NEW
    </h1>
  );
};

// New.propTypes = {}

export default New;
